# GraphQL Server Definitions

This package contains the GraphQL server definitions for the application.

## Development

To run the development server, use the following command:

```bash
npm run dev
```

## Build

To build the production server, use the following command:

```bash
npm run build
```

## Generate Types

To generate the GraphQL types, use the following command:

```bash
npm run generate
```
